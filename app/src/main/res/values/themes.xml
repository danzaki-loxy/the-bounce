<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Core dark theme -->
        <item name="android:windowBackground">@color/color_background</item>
        <item name="android:colorBackground">@color/color_background</item>
        <item name="android:statusBarColor">@color/color_background</item>
        <item name="android:navigationBarColor">@color/color_background</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- AppCompat color overrides -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_variant</item>
        <item name="colorAccent">@color/color_accent</item>

        <!-- Text & Icon colors (adapt for dark mode) -->
        <item name="android:textColorPrimary">@color/color_on_background</item>
        <item name="android:textColorSecondary">@color/on_dark</item>
        <item name="android:textColorHint">@color/light_grey</item>
        <item name="colorControlNormal">@color/color_on_background</item>
        <item name="colorControlActivated">@color/color_primary</item>
        <item name="colorControlHighlight">@color/blur_overlay</item>

        <!-- Surface & Card backgrounds -->
        <item name="android:colorForeground">@color/color_on_surface</item>
        <item name="cardBackgroundColor">@color/color_surface</item>
    </style>
</resources>